# 桌面端问题复盘

记录 2026-06-29 这轮桌面端测试中暴露的问题、根因和后续约定，用于避免同类问题反复出现。

## 1. 透明边框和窗口尺寸

现象：
- 完整模式或精简模式边缘出现透明区域。
- 从精简模式恢复完整模式后，UI 偶尔变窄。
- 完整模式手动调整窗口后，透明区域再次出现。

根因：
- Tauri 窗口使用 `transparent: true` 和 `decorations: false`，窗口比内容大时会直接露出透明 WebView。
- 之前在 `renderAll()` 中按内容尺寸反复调用 `set_window_size`，和用户手动拖拽缩放互相抢控制权。
- 旧版用 `outer-inner` 差值补偿窗口尺寸，在无边框透明窗口中反而会把窗口撑大。

约定：
- 完整模式容器必须填满 Tauri 窗口，不能用固定内容宽高去适配外层窗口。
- 完整模式允许用户缩放，JS 不在常规渲染中自动改完整模式窗口尺寸。
- 精简模式才使用固定尺寸，并在切换时主动设置窗口大小。
- 默认完整模式尺寸为 `350x600`，旧默认尺寸缓存需要迁移。

## 2. 模式切换导致任务终止

现象：
- 展开和精简切换后，常驻任务或自定义任务全部停止。

根因：
- 新增自定义任务或切换相关流程曾调用 `initTasks()` 重建 `tasks`，导致运行态丢失。
- Tauri 窗口变化可能触发 WebView 状态波动，运行态只存在内存里不够稳。

约定：
- 新增自定义任务只增量添加任务状态，不重建全部任务。
- 需要重建任务列表时，先保存同 ID 任务的 `running/remaining/isPrimary`，重建后再迁移回来。
- 运行态使用单独的 `mmy_tasktime_runtime` 保存，包含当前模式、完整模式尺寸、运行任务和剩余时间。

## 3. 前端资产目录

现象：
- `npm run build` 或 `cargo check` 报错，提示 `frontendDist` 包含 `src-tauri/target`、`node_modules` 或锁文件。

根因：
- `frontendDist` 曾配置为 `../`，Tauri 会把项目根目录整体当成前端资产扫描。

约定：
- 使用 `dist/` 作为唯一前端产物目录。
- `npm run prepare:dist` 只复制 `index.html`、`style.css`、`app.js`。
- `tauri.conf.json > build.frontendDist` 固定为 `../dist`。

## 4. UI 设计经验

完整模式：
- 顶部使用左右分栏专注面板：左侧真实模拟时钟，右侧主任务倒计时。
- 主任务进度使用整块透明填充，不使用过细进度条。
- 常驻任务按钮直接显示运行中剩余时间，避免顶部运行条重复展示常驻任务。
- 常驻任务按钮文字必须居中，并和区块标题保持足够距离。

精简模式：
- 默认跟随浅色主题，避免在浅色环境里显得像反色。
- 黑白模式通过底部 `◐/◑` 按钮切换，完整模式和精简模式共用同一个主题设置。
- 精简模式显示“下个到点”信息，避免底部空白。

设置面板：
- 常驻任务开放 4 个固定槽位编辑：名称、分钟数、是否循环。
- 保留固定 ID（`q5/q10/q30/q40`），避免破坏托盘快捷入口和历史运行逻辑。
- 保存常驻任务设置后立即生效，并尽量保留同 ID 的运行状态。

## 5. 发布检查

发布前至少执行：

```bash
node --check app.js
npm run build
```

单文件可执行程序来自：

```text
src-tauri/target/release/bundle/nsis/MMY-TaskTime_2.0.0_x64-setup.exe
# 或单文件（无安装器）
src-tauri/target/release/mmy-tasktime.exe
```

对外交付时复制到：

```text
releases/MMY-TaskTime-v版本号.exe
```

## 6. 深色模式文字对比度问题（2026-06-29）

现象：
- 深色模式下，常驻任务按钮（loop 橙色 / water 绿色）的文字颜色为白色 `#f4f4f5`，
  但按钮背景在 `running` / `primary-active` 状态下仍为浅色（`#fff7ed` / `#edf9f0`），
  导致白字在浅底上几乎不可见。

根因：
- `theme-dark` 的 CSS 只覆盖了通用文字颜色（`#f4f4f5`），但没有针对 `.running.loop`、
  `.primary-active.loop`、`.running.water`、`.primary-active.water` 这些组合状态
  单独设置对比度足够的文字色和背景色。

约定：
- 深色模式下，所有带颜色语义的按钮状态（loop / water / primary）都必须有对应的
  `body.theme-dark` 覆盖样式，确保文字与背景对比度 ≥ 4.5:1。
- 修改 `style.css` 的深色主题区块时，同步检查完整模式底栏、常驻任务按钮、设置面板
  中所有带颜色类的元素。

## 7. 图标语义混淆（2026-06-29）

现象：
- 设置按钮使用 ⚙（齿轮）图标，在深色模式下容易被误认为是「日间模式」或「太阳」图标。

根因：
- ⚙ 在较小字号（15px）渲染时，齿牙细节丢失，看起来像一个实心圆或太阳。

约定：
- 设置入口统一使用 🔧（扳手）图标，语义明确，不会与主题切换按钮（◐/◑）混淆。
- 图标变更需同步更新 `index.html`、`dist/index.html`、`mini-preview.html`、`README.md` 四处。

## 8. 精简模式冗余图标（2026-06-29）

现象：
- 精简模式下时间前方有一个 🕙 时钟图标，在窄宽度（156px）里占用空间且信息冗余
  （时间数字本身已足够表达）。

根因：
- `.mini-mode .clock-icon` 沿用了完整模式的样式，未针对精简模式做隐藏处理。

约定：
- 精简模式只保留最核心信息（时间 + 主任务名 + 倒计时），装饰性图标一律隐藏。
- 通过 `.mini-mode .clock-icon { display: none; }` 实现，不删除 HTML 节点，
  方便将来需要时通过 CSS 恢复。

## 9. 一键打包脚本（2026-06-29）

现象：
- 双击 `build-win.bat` 后出现大量乱码和语法错误。
- 错误提示包括：`'0' 不是内部或外部命令`、`'//rustup.rs/' 不是内部或外部命令`、`'pause' 不是内部或外部命令` 等。
- 中文显示为乱码（如 `'寘搴旂敤...'`、`'鏈壘鍒扮紪璇译繃'`）。

根因：
- **编码问题**：批处理文件包含中文字符，但未正确处理 UTF-8 编码。Windows 命令行默认使用 GBK 编码，导致中文乱码。
- **语法错误**：`echo` 语句中包含特殊字符（如 `()`）且未正确转义，导致批处理解析失败。
- **变量处理**：未使用 `setlocal EnableDelayedExpansion`，导致变量在 `if` 块中无法正确展开。
- **版本号缺失**：便携版可执行文件未包含版本号，不利于版本管理。

解决方案：
- **统一使用英文输出**：避免在批处理文件中使用中文，彻底解决编码问题。
- **简化输出信息**：移除 `echo` 语句中的特殊字符，使用简单的纯文本输出。
- **正确错误处理**：使用 `if %errorlevel% neq 0` 进行错误判断，避免使用复杂语法。
- **自动版本号**：从 `package.json` 自动读取版本号，生成带版本号的可执行文件（如 `mmy-tasktime-v2.0.0.exe`）。

约定：
- **跨平台打包脚本**：
  - Windows：使用 `.bat` 批处理脚本（`build-win.bat`、`build-portable-win.bat`）
  - macOS/Linux：使用 `.sh` Shell 脚本（`build-mac.sh`、`build-portable-mac.sh`）
- **版本号管理**：
  - 版本号统一在 `package.json` 和 `src-tauri/tauri.conf.json` 中维护
  - 打包脚本自动读取版本号，生成带版本号的文件
  - 格式：`mmy-tasktime-v{版本号}.exe`
- **输出目录**：
  - 安装包：`src-tauri/target/release/bundle/`
  - 便携版：`release-portable/`
- **脚本测试**：每次修改打包脚本后，必须在对应平台测试通过才能提交。

## 变更记录

### 2026-06-29
- 精简模式时间区改为横向专注面板：左侧显示小指针钟、日期与「专注工作 · 心流时刻」，右侧显示主任务名、倒计时和停止按钮。
- 精简模式进度展示从顶部细条调整为任务区背景填充，并同步浅色主题覆盖。
- 同步更新 `mini-preview.html` 预览页与 `dist` 前端产物，确保原型和 Tauri 运行资源一致。
- 创建一键打包脚本（Windows/Mac 双平台），支持自动版本号和便携版生成。
- 修复批处理文件编码和语法问题，统一使用英文输出避免乱码。
